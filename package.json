{
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --static src",
    "watch": "browser-sync start --proxy localhost:4000 --files src",
    "dev": "run-p start watch"
  },
  "dependencies": {
    "json-server": "^0.16.1"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "npm-run-all": "^4.1.5"
  }
}
